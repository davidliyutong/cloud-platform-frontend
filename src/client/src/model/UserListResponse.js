/**
 * API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import UserModel from './UserModel';

/**
 * The UserListResponse model module.
 * @module model/UserListResponse
 * @version 1.0.0
 */
class UserListResponse {
    /**
     * Constructs a new <code>UserListResponse</code>.
     *      List response for users     
     * @alias module:model/UserListResponse
     * @param status {Number} 
     * @param message {String} 
     */
    constructor(status, message) { 
        
        UserListResponse.initialize(this, status, message);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, status, message) { 
        obj['status'] = status;
        obj['message'] = message;
    }

    /**
     * Constructs a <code>UserListResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UserListResponse} obj Optional instance to populate.
     * @return {module:model/UserListResponse} The populated <code>UserListResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UserListResponse();

            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'Number');
            }
            if (data.hasOwnProperty('message')) {
                obj['message'] = ApiClient.convertToType(data['message'], 'String');
            }
            if (data.hasOwnProperty('total_users')) {
                obj['total_users'] = ApiClient.convertToType(data['total_users'], 'Number');
            }
            if (data.hasOwnProperty('users')) {
                obj['users'] = ApiClient.convertToType(data['users'], [UserModel]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UserListResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UserListResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UserListResponse.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['message'] && !(typeof data['message'] === 'string' || data['message'] instanceof String)) {
            throw new Error("Expected the field `message` to be a primitive type in the JSON string but got " + data['message']);
        }
        if (data['users']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['users'])) {
                throw new Error("Expected the field `users` to be an array in the JSON data but got " + data['users']);
            }
            // validate the optional field `users` (array)
            for (const item of data['users']) {
                UserModel.validateJSON(item);
            };
        }

        return true;
    }


}

UserListResponse.RequiredProperties = ["status", "message"];

/**
 * @member {String} description
 * @default ''
 */
UserListResponse.prototype['description'] = '';

/**
 * @member {Number} status
 */
UserListResponse.prototype['status'] = undefined;

/**
 * @member {String} message
 */
UserListResponse.prototype['message'] = undefined;

/**
 * @member {Number} total_users
 * @default 0
 */
UserListResponse.prototype['total_users'] = 0;

/**
 * @member {Array.<module:model/UserModel>} users
 */
UserListResponse.prototype['users'] = undefined;






export default UserListResponse;

