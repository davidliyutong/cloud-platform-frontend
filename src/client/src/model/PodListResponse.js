/**
 * API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PodModel from './PodModel';

/**
 * The PodListResponse model module.
 * @module model/PodListResponse
 * @version 1.0.0
 */
class PodListResponse {
    /**
     * Constructs a new <code>PodListResponse</code>.
     *      List response for pods     
     * @alias module:model/PodListResponse
     * @param status {Number} 
     * @param message {String} 
     */
    constructor(status, message) { 
        
        PodListResponse.initialize(this, status, message);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, status, message) { 
        obj['status'] = status;
        obj['message'] = message;
    }

    /**
     * Constructs a <code>PodListResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PodListResponse} obj Optional instance to populate.
     * @return {module:model/PodListResponse} The populated <code>PodListResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PodListResponse();

            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'Number');
            }
            if (data.hasOwnProperty('message')) {
                obj['message'] = ApiClient.convertToType(data['message'], 'String');
            }
            if (data.hasOwnProperty('total_pods')) {
                obj['total_pods'] = ApiClient.convertToType(data['total_pods'], 'Number');
            }
            if (data.hasOwnProperty('pods')) {
                obj['pods'] = ApiClient.convertToType(data['pods'], [PodModel]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PodListResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PodListResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PodListResponse.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['message'] && !(typeof data['message'] === 'string' || data['message'] instanceof String)) {
            throw new Error("Expected the field `message` to be a primitive type in the JSON string but got " + data['message']);
        }
        if (data['pods']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['pods'])) {
                throw new Error("Expected the field `pods` to be an array in the JSON data but got " + data['pods']);
            }
            // validate the optional field `pods` (array)
            for (const item of data['pods']) {
                PodModel.validateJSON(item);
            };
        }

        return true;
    }


}

PodListResponse.RequiredProperties = ["status", "message"];

/**
 * @member {String} description
 * @default ''
 */
PodListResponse.prototype['description'] = '';

/**
 * @member {Number} status
 */
PodListResponse.prototype['status'] = undefined;

/**
 * @member {String} message
 */
PodListResponse.prototype['message'] = undefined;

/**
 * @member {Number} total_pods
 * @default 0
 */
PodListResponse.prototype['total_pods'] = 0;

/**
 * @member {Array.<module:model/PodModel>} pods
 */
PodListResponse.prototype['pods'] = undefined;






export default PodListResponse;

